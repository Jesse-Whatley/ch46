class Task {
    constructor(title,description,color,startDate,status,budget){
        this.title = title;
        this.description = description;
        this.color = color;
        this.startDate = startDate;
        this.status = status;
        this.budget = budget
    }
}

// attributes ==> help describe the object (ex:car== color)
// method ==> anything the object can do (ex:car== headlights, lock door)